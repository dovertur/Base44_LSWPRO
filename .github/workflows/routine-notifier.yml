name: Routine Notifier (Base44)

on:
  schedule:
    - cron: "0 */4 * * *"  # Every 4 hours, at minute 0 (UTC)
  workflow_dispatch:

jobs:
  call-routine-notifier:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger routineNotifier
        run: |
          set -e
          curl -sS -X POST "https://base44.app/api/apps/68c07b3fd994be317e5743df/functions/routineNotifier" \
            -H "Content-Type: application/json" \
            ${BASE44_API_KEY:+-H "Authorization: Bearer $BASE44_API_KEY"} \
            -d '{}'
        env:
          BASE44_API_KEY: ${{ secrets.BASE44_API_KEY }}
